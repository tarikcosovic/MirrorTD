using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Types : MonoBehaviour
{
    public enum AttackTypes { Normal, Pierce, Magic, Splash };

    public enum ArmorTypes { None, Light, Normal, Medium, Magic, Fortified, Siege };

    public enum Races { Gelefor, Zhundavi, Necros, Hylfir };
}
